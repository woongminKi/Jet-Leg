# 2026-05-03 W17 4 Day + W17 종합 + W18 진입 핸드오프

> **다음 세션 5분 안 진입 자료.**
> 마지막 commit: `e85abc4` (origin/main)

---

## 0. 한 줄 요약

W17 4 day — MetricsTrendCard interactivity + vision 시계열 카드 추가 + 한계 #85·#88 동시 회수 (first-warn 패턴 + 비동기 persist). 단위 테스트 **254 → 257** ran. **한계 회수 누적 33 → 35**. **외부 의존성 0 유지**.

---

## 1. W17 4 Day 종합

| Day | commit | 본질 | 단위 테스트 | 한계 |
|---|---|---|---:|---|
| Day 1 | `b2f7d8d` | MetricsTrendCard 'use client' + range 토글 + Server initial / Client refetch 패턴 | 254 | (UX 개선) |
| Day 2 | `5875631` | vision metric 시계열 카드 추가 (RPD 20 cap 가시성) | 254 | (UX 개선) |
| Day 3 | `1ae24f6` | vision_metrics + search_metrics first-warn 패턴 (#85 회수) | 257 | #85 |
| Day 4 | `e85abc4` | metrics persist ThreadPoolExecutor 비동기 fire-and-forget (#88 회수) | 257 | #88 |
| **Day 5** | **(본 문서)** | **W17 종합 + W18 진입 핸드오프** | 257 | — |

### 1.1 W17 누적 변경

- **commit**: 5 (4 코드 + 1 핸드오프)
- **단위 테스트**: 254 → **257** (+3: FirstWarn vision 2 + search 1)
- **한계 회수 누적**: 33 → **35** (+ #85·#88)
- **마이그레이션**: 7 (변동 없음)
- **외부 의존성 추가**: **0** ('use client' 만 추가, useSWR/recharts 회피)
- **신규 모듈/패턴**:
  - `MetricsTrendCard` 'use client' 전환 (Server initial + Client refetch + race 방지 cancelled flag)
  - vision_metrics + search_metrics dispatcher / sync 분리 (ThreadPoolExecutor lazy init)
  - tests/__init__.py + 두 모듈 head 에 ENABLED/ASYNC env 강제 (discover top-level-dir 무관)

### 1.2 비판적 재검토 (W17)

| Day | 발견 |
|---|---|
| Day 1 | useSWR (~30KB) vs stdlib React — **stdlib 채택**. polling 미필요, 단순 mount-once fetch + 토글 refetch 패턴이면 충분. 외부 의존성 0 정책 일관 (W16 §6.32 SVG 직접 그리기와 동일) |
| Day 1 | React 19 lint `react-hooks/set-state-in-effect` — useEffect 동기 setState 금지. **handler 분리 패턴** (handleRangeChange 가 isLoading/fetchError 동기 처리, useEffect 는 fetch + .then 비동기 setState 만) |
| Day 1 | derived state 패턴 — fetchedTrend 와 range 일치 검사 후 trend 결정. range 토글 빠르게 시 stale data 회피 |
| Day 2 | search·vision 한 카드 통합 vs 분리 — **분리 채택**. Y축 단위 다름 (p95 ms vs 호출 수), sparkline 직접 비교 가치 낮음. 토글 1번 누르고 다른 metric 보는 비용 < 두 카드 동시 시야 가치 |
| Day 3 | debug→info 일괄 승격 (#85 원안) vs first-warn 패턴 — **first-warn 채택**. 매 호출 info 시 로그 노이즈 폭증, 한 번만 명시 알림 + 이후 debug 가 균형. 단순 bool flag (race 시 warn 두 번 정도, 정합성 영향 0) |
| Day 4 | daemon=False thread (Python 3.9+ default) — 운영 종료 시 wait. graceful 종료 우선 vs atexit latency. 가치 우위 |
| Day 4 | unittest discover --top-level-directory 미명시 시 tests/__init__.py 안 잡힘 — **각 테스트 파일 head 에 env 강제 set 보호**. discover 호출 패턴 변경 없이 robustness↑ |

---

## 2. 누적 KPI (W17 Day 5 마감)

| KPI | 목표 | 결과 | 충족 |
|---|---|---|---|
| HWPX section_title | ≥30% | **100%** | ✅ |
| PDF section_title | ≥30% | **99.84%** | ✅ |
| 출처 일치율 | ≥0.95 | **1.0** | ✅ |
| P95 검색 응답 | ≤3초 | **219ms** (+α: persist async 로 보호 강화) | ✅ |
| **시계열 추세 시각화** | — | **search + vision 카드 + range 토글** | ✅ |
| 인제스트 SLO 달성률 | ≥90% | `/stats.ingest_slo_aggregate` | ✅ |
| 단위 테스트 | 0 회귀 | **257/257** | ✅ |
| 유저 스토리 | 8건 | **8/8** | ✅ |
| e2e | — | **9/9 + extract 5/5 + 스캔 PDF cap** | ✅ |
| metrics 영속화 + 비동기 | — | DB write-through + graceful + RPC + ThreadPoolExecutor | ✅ |

### 2.1 commit 누적

W3~W16 의 누적 + W17·5 = 약 100. (정확한 카운트는 `git log --oneline | wc -l` 로 재확인 권장.)

### 2.2 한계 회수 누적: **35건**

| 분류 | 한계 # |
|---|---|
| W7~W14 (24건) | 이전 핸드오프 §2.2 |
| W15 (6건) | #34·#61·#62·#64·#76·#81 |
| W16 (3건) | #40·#84·#90 |
| **W17 (2건)** | **#85** vision_metrics + search_metrics first-warn 패턴 / **#88** _persist_to_db 비동기 fire-and-forget (검색 latency 보호) |

---

## 3. 환경 + 진입 절차

### 3.1 저장소

```bash
git pull origin main  # e85abc4
cd api && uv run python -m unittest discover tests  # 257 ran (회귀 0)
cd ../web && pnpm exec tsc --noEmit && pnpm lint     # 0 error
```

### 3.2 마이그레이션 적용 상태

| # | 파일 | 적용 |
|---|---|---|
| 001~004 | (W2~W3) | ✅ |
| 005·006·007 | vision_usage_log + search_metrics_log + 추세 RPC | 🟡 **사용자 Studio 적용 대기** |

### 3.3 사용자 액션

1. Supabase Studio → SQL Editor → 005·006·007 순차 paste & Run
2. uvicorn 재시작 (env `JET_RAG_METRICS_PERSIST_ENABLED='1'` default 활성, ASYNC='1' default — fire-and-forget)
3. 홈 dashboard → "검색 응답 추세" + "Vision API 호출 추세" 카드 자동 노출
4. range 토글 (24h/7d/30d) — client refetch
5. 검색 실행 누적 → sparkline 데이터 채워짐

### 3.4 운영 환경 변수 (W15~W17 누적)

| env | default | 효과 |
|---|---|---|
| `JET_RAG_METRICS_PERSIST_ENABLED` | `"1"` | DB write-through 활성/비활성 |
| `JET_RAG_METRICS_PERSIST_ASYNC` | `"1"` | ThreadPoolExecutor fire-and-forget vs sync |
| `JET_RAG_VISION_ERROR_MSG_MAX_LEN` | `"200"` | error_msg DB row 크기 제한 |

---

## 4. W18 우선순위

### 4.1 메인 스레드 가능

| 우선 | 항목 | 비용 |
|---|---|---|
| 1 | **MetricsTrendCard mode 토글** (search 카드, all/hybrid/dense/sparse) | ~1h |
| 2 | **DoD ④ OpenAI 어댑터 스왑 시연** (사용자 보류 해제 시) | ~3h |
| 3 | **augment 본 검증** | quota 회복 |
| 4 | **마이그레이션 005·006·007 적용 후 시계열 시연 영상** | 사용자 환경 |
| 5 | **#87 query_text hash 화** (DE-21 사전 작업, 멀티 유저 대비) | ~1h |
| 6 | **한계 #65/#74/#75 안정 가정 회수** (테스트 robustness) | ~1~2h |
| 7 | **`_persist_executor` graceful shutdown** (atexit hook) | ~30분 |

### 4.2 사용자 협조 필요

| 항목 | 액션 | DoD |
|---|---|---|
| 마이그레이션 005·006·007 적용 | Studio direct apply | W15·W16·W17 자산 활성 |
| Ragas 의존성 승인 | `uv add ragas` | KPI ②·③ |
| 평가 데이터셋 45건 | HWP/PDF/스샷/DOCX | KPI ② |
| Golden Path 영상 | 시나리오 녹화 | KPI ⑤ |
| 4 플랫폼 QA | Win/Mac/Android/iOS | KPI ⑥ |
| GitHub Actions secrets | `JET_RAG_API_BASE` | monitor cron |

### 4.3 추천 sprint — W18 진입

**Option A (mode 토글, ~1h)**: MetricsTrendCard 의 search 모드에서 mode (all/hybrid/dense/sparse) 토글. range 토글 패턴 재사용. 사용자가 ablation 시계열 비교 가능.

**Option B (#87 hash 화 사전, ~1h)**: search_metrics_log.query_text 의 hash 옵션 wiring. env `JET_RAG_QUERY_TEXT_HASH` 도입. 멀티 유저 진입 시 즉시 활성 가능.

**Option C (안정 가정 회수, ~2h)**: #65 ImageParser.parse class-level patch → 인스턴스 patch 로 변경 + #74·#75 ablation top-K cap 검증.

**메인 추천: Option A → B 묶음 (~2h)** — 사용자 ablation 가시성 + 멀티 유저 사전 작업.

비판적 재검토 — Option B 의 hash 화는 단일 사용자 MVP 단계 가치 낮으나 wiring 만 해두면 W19+ 의 큰 sprint 회피. ROI 균형.

---

## 5. 활성 한계 (W17 마감, ~20건)

회수 완료 (35건) — §2.2 참조.

활성 주요 (~20건):
- #38 본격 (Gemini SDK quota header)
- #48 augment 본 검증 (quota 회복)
- #56·#57 SDK 변경 가정
- #65·#70~#75·#79·#80·#82·#86·#87·#89 작은 trade-off

---

## 6. 운영 정책 (W3~W17 누적)

W16 §6 의 36개 + W17 신규:

37. **Server initial + Client refetch 패턴** — RSC 가 첫 SSR 데이터 prop 전달, 'use client' 자식이 useState 초기값 + 토글 시 client fetch. SSR HTML 즉시 표시 + 사용자 interactivity (W17 Day 1)
38. **derived state + race 방지 cancelled flag** — fetchedTrend?.range === range 검사로 stale data 회피. useEffect cleanup return cancelled 패턴 (W17 Day 1)
39. **first-warn 패턴** — DB persist 실패 첫 1회만 warn, 이후 debug. 사용자 인지 + 로그 노이즈 균형. 단순 bool flag (race 영향 0) (W17 Day 3)
40. **ThreadPoolExecutor lazy init + fire-and-forget** — metrics persist 비동기 dispatch 로 호출자 latency 보호. ENV ASYNC='0' 시 sync 강제 (단위 테스트 capture race 회피) (W17 Day 4)
41. **테스트 env gate 이중화** — tests/__init__.py + 각 test 파일 head 동시에 env 강제 set. discover top-level-dir 미명시 시 robustness 보호 (W17 Day 4)

자율 진행 v2 정착 회고: W11 5d + W12 3d + W13 4d + W14 5d + W15 4d + W16 4d + **W17 4d = 29 day** 모두 사용자 확인 0 패턴 정상 작동.

---

## 7. 다음 작업자 권장 읽기 순서

| # | 문서 | 목적 |
|---|---|---|
| 1 | `README.md` | 프로젝트 진입 |
| 2 | `2026-04-22 개인 지식 에이전트 기획서 v0.1.md` | 마스터 |
| 3 | **`2026-05-03 W17 종합 + W18 진입 핸드오프.md`** (본 문서) | 즉시 진입 |
| 4 | `2026-05-03 W16 종합 + W17 진입 핸드오프.md` | 직전 W |
| 5 | `web/src/components/jet-rag/cards/metrics-trend-card.tsx` | 'use client' + Server initial 패턴 |
| 6 | `api/app/services/vision_metrics.py` (`_persist_to_db` dispatcher) | ThreadPoolExecutor 비동기 패턴 |
| 7 | `api/migrations/README.md` | 005·006·007 적용 가이드 |
| 8 | `/Users/kiwoongmin/Desktop/piLab/CLAUDE.md` | 자율 진행 v2 |
| 9 | `web/AGENTS.md` | Next.js 16 |

---

## 8. 한 문장 요약

W17 4 day — MetricsTrendCard interactivity ('use client' + range 토글) + vision 시계열 카드 추가 + 한계 #85·#88 동시 회수 (first-warn 패턴 + ThreadPoolExecutor 비동기 persist). 단위 테스트 254 → **257**, **외부 의존성 0 유지**. 자율 진행 v2 29 day 정착. W18 진입 추천: **Option A+B (mode 토글 + #87 hash 화 사전)**.

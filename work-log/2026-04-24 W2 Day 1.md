# 2026-04-24 W2 Day 1 (선행 착수)

> W2 공식 일자는 2026-04-29 ~ 2026-05-05. 사용자 요청으로 **Day 1 일부를 2026-04-24 당일 선행 착수**. 목표: 의존성 설치 검증 + DE-23 Gemini 2.5 교체 + SLO 회복 기반 준비 + VisionCaptioner 골격 + 평가셋 시드.

> **근거 문서**: `work-log/2026-04-24 W2 스프린트 명세.md` v0.2 (CONFIRMED) §7

---

## 0. 실시간 진행 상태

| # | 단계 | 상태 |
|---|---|---|
| 1 | 환경 조사 (api/web 주요 파일, Gemini 호출부, pyproject.toml) | ✅ |
| 2 | `uv add` 의존성 3종 설치 및 Python 3.12 호환성 검증 | ✅ |
| 3 | DE-23 Gemini 2.5 Flash 확인 (이미 반영됨) + docstring 정리 | ✅ |
| 4 | VisionCaptioner Protocol 분리 (adapters/vision.py + impl/gemini_vision.py 신설) | ✅ |
| 5 | `/evals/{hwpx,vision,pdf}_samples/` 디렉토리 구조 + README + expected schema | ✅ |
| 6 | 명세 v0.2 § 3.A′ 보정 (`doc_type="pdf_scan"` → `flags.scan=true`) · DB CHECK 위반 방지 | ✅ |
| 7 | **[이관] 8-stage 파이프라인 — content_gate 신설 + STAGE_ORDER 갱신** | → Day 5 (G 구현 시 같이) |
| 8 | **[이관] SLO 회복 — pending path + SHA-256 스트리밍 + file magic + 50MB 카운터** | → W2 Day 2 |
| 9 | **[이관] `/stats.slo_buckets` 집계 필드 추가** | → W2 Day 2 |
| 10 | Day 1 본 문서 완성 + 커밋 단위 분할 + push | ⏳ |

> Day 1 선행 착수 범위는 **단계 1~6 마감**. 단계 7~9 는 SLO 회복과 content_gate 스테이지 추가가 큰 구조 변경이라 하루 내에 무리. W2 공식 개시일 (2026-04-29) 의 **Day 2 첫 스탠드업 대신** 바로 착수 가능하도록 명세·어댑터·의존성·평가 스키마만 정돈.

---

## 1. 환경 조사 결과

### 1.1 api (백엔드)

| 파일 | 핵심 발견 |
|---|---|
| `pyproject.toml` | Python 3.12+, fastapi/google-genai/pymupdf/supabase 기반. 의존성 minimal |
| `app/routers/documents.py` | `POST /documents` 가 이미 SHA-256 계산 + Tier 1 dedup + failed 자동 reingest 분기 구현. 단 `data = await file.read()` 로 전체 메모리 적재 → 50MB 스트리밍 카운터 부재 |
| `app/ingest/pipeline.py` | 7-stage 그대로. 실패 시 `_cleanup_failed_doc` 로 chunks 정리 + flags 마킹 |
| `app/ingest/stages/extract.py` | 비 PDF 는 graceful skip + `flags.extract_skipped=true` 마킹. Day 4 이후 "스캔본 → Vision 재라우팅" 은 TODO |
| `app/adapters/parser.py` | `DocumentParser` Protocol + `ExtractionResult` 데이터클래스. `can_parse(file_name, mime)` 디스패처 후크 |
| `app/adapters/llm.py` | `LLMProvider` Protocol (Gemini 2.0 Flash docstring — 구현 실제는 2.5). `complete(messages, images, ...)` 에 Vision 지원 포함 |
| `app/adapters/impl/gemini_llm.py` | **`_DEFAULT_MODEL = "gemini-2.5-flash"` 이미 반영됨**. DE-23 사실상 구현 완료. 단 `_attach_images` 가 모든 이미지를 `image/png` 로 하드코딩 → HEIC/JPEG/WebP 전달 시 잘못된 mime |
| `app/adapters/impl/` | `pymupdf_parser / supabase_storage / supabase_vectorstore / bgem3_hf_embedding / gemini_llm` 5개 구현체 |
| `migrations/001_init.sql` | `documents.storage_path TEXT NOT NULL` · `doc_type CHECK IN ('pdf','hwp','hwpx','docx','pptx','image','url','txt','md')` · `source_channel CHECK IN ('drag-drop','os-share','clipboard','url','camera','api')` · `ingest_jobs.current_stage TEXT` (free) |

**중요 발견**:
1. **`ingest_jobs.current_stage` 는 free TEXT** → 8-stage 확장 시 DB migration 불필요
2. **`documents.doc_type` CHECK 에 `pdf_scan` 없음** → 명세 §3.A′ 의 `doc_type="pdf_scan"` 은 CHECK 위반. **`doc_type="pdf"` 유지 + `flags.scan=true` 로 구분** 하도록 명세 조정 필요
3. **`source_channel` 은 기존 6종 유지**. 이미지 구분은 `doc_type="image"` (이미 enum 에 존재) — 명세 §3.D E-4 이미 반영됨
4. **Gemini `_attach_images` mime 하드코딩** → Vision 경로 구현 시 함께 유연화 필요

### 1.2 web (프론트)

| 파일 | 핵심 발견 |
|---|---|
| `AGENTS.md` | **"This is NOT the Next.js you know"** 경고. Next.js 16 신 API 로 학습 데이터와 다름. 수정 전 `node_modules/next/dist/docs/` 확인 요구 |
| `src/lib/api/types.ts` | `StageValue` 유니온에 `'content_gate'` 없음 → 추가 필요. `DocType` 유니온에 `'pdf_scan'` 없음 (명세 조정으로 불요) |
| `src/lib/stages.ts` | `STAGE_ORDER` 7개. `content_gate` 자리 2번째 (chunk 이후 tag_summarize 이전) |

### 1.3 명세 v0.2 수정 필요사항 (환경 조사 기반)

- **§3.A′ 스캔 PDF 재라우팅** — `documents.doc_type="pdf_scan"` → `documents.doc_type="pdf" + flags.scan=true` 로 변경. DB CHECK 제약 존중
- **§3.B Vision** — `_attach_images` mime 하드코딩 수정 항목 추가

---

## 2. 의존성 설치 로그

### 2.1 설치 결과 (2026-04-24)

```
uv add trafilatura python-hwpx pyhwp
```

| 패키지 | PyPI 이름 | Python import 이름 | CLI | 버전 | Python 3.12 호환 |
|---|---|---|---|---|---|
| trafilatura | trafilatura | `trafilatura` | — | 2.0.0 | ✅ |
| python-hwpx | python-hwpx | `hwpx` | — | 2.9.0 | ✅ |
| pyhwp | pyhwp | **`hwp5`** | `hwp5html` | 0.1b15 | ✅ |

**주의 — pyhwp 는 PyPI 이름과 import 이름이 다름**:
- PyPI: `pyhwp`
- Python: `from hwp5 import binmodel` (NOT `import pyhwp`)
- CLI: `hwp5html --version` 통과

DE-13 허용치 격상 (HWPX 1등, HWP 제외) **불필요** — pyhwp Python 3.12 호환성 확인됨.

부수 설치 패키지: `olefile` (pyhwp), `lxml · lxml-html-clean · courlan · dateparser · htmldate · justext · tld · babel · pytz · regex · tzlocal` (trafilatura)

### 2.2 pyproject.toml 확정

```toml
dependencies = [
    "fastapi>=0.136.0",
    "google-genai>=1.0.0",
    "pyhwp>=0.1b15",
    "pymupdf>=1.24.0",
    "python-dotenv>=1.2.2",
    "python-hwpx>=2.9.0",
    "python-multipart>=0.0.19",
    "supabase>=2.11.0",
    "trafilatura>=2.0.0",
    "uvicorn[standard]>=0.45.0",
]
```

### 2.3 DE-20 · DE-17 상태 갱신

- **DE-20 RESOLVED** — pyhwp Python 3.12 완전 호환. Railway 배포 시에도 Nixpacks Python phase 가 자동 처리
- **DE-17 RESOLVED** (기존 확정) — Gemini HEIC 직접 지원 ✓, 단 `_attach_images` 의 mime 하드코딩은 Vision 구현 시 동시 수정 필요

---

## 3. 구현 로그

### 3.1 DE-23 Gemini 2.5 Flash docstring 정리

- `api/app/adapters/impl/gemini_llm.py` — 파일 헤더 docstring 및 `GeminiLLMProvider` 클래스 docstring 에서 "Gemini 2.0 Flash" → "Gemini 2.5 Flash" 로 교정. 실제 `_DEFAULT_MODEL` 상수는 이미 `gemini-2.5-flash` 였음 (오인용된 주석만 교정)
- `api/app/adapters/llm.py` — `LLMProvider` Protocol docstring 교정. Vision 책임은 `VisionCaptioner` 로 분리됐음 명시 (DE-19)
- 실제 모델 교체 구현 작업 없음 — Day 1 Gemini 호출 regression 별도 불요

### 3.2 VisionCaptioner 어댑터 신설 (DE-19 반영)

- `api/app/adapters/vision.py` (신규)
  - `VisionCategory` Literal 8종 (§10.4)
  - `VisionCaption` frozen dataclass — 4필드 (`type / ocr_text / caption / structured`). `raw_confidence` 는 QA 검수 B-1 에 따라 Day 2 실응답 실측 후 v0.3 결정 예정 — docstring 에 명시
  - `VisionCaptioner` Protocol — `caption(image_bytes, *, mime_type)`
- `api/app/adapters/impl/gemini_vision.py` (신규)
  - `GeminiVisionCaptioner` 스켈레톤 (`NotImplementedError` raise). Day 2 실제 구현
  - docstring 에 "EXIF transpose / orientation 제거 / 다운스케일 은 ImageParser 책임" (C-2 단일 책임) 명시
  - HEIC/HEIF 는 Gemini 2.5 Flash 직접 지원 (DE-17 RESOLVED) 명시
- `api/app/adapters/__init__.py` — `VisionCaptioner / VisionCaption / VisionCategory` export 추가
- smoke test 통과: `uv run python -c "from app.adapters import VisionCaptioner, VisionCaption; from app.adapters.impl.gemini_vision import GeminiVisionCaptioner"`

### 3.3 /evals/ 디렉토리 + 기대값 스키마

- `evals/README.md` — W2 평가셋 개요, 성공 정의, 수집 가이드라인
- `evals/vision_samples/expected.schema.json` — `expected.jsonl` 의 JSON Schema (Draft 2020-12). `type` enum 8종, `ocr_ground_truth` / `structured_ground_truth` / `source_url` / `notes` 필드
- `evals/{hwpx_samples,pdf_samples}/.gitkeep` — Day 2 수집 대기
- 실제 샘플 수집은 W2 Day 2 아침 예정 (파일 크기 제어 별도 PR 검토)

### 3.4 명세 v0.2 보정

- §3.A′ 스캔 PDF 재라우팅 — `documents.doc_type="pdf_scan"` (DB CHECK 위반) → `documents.doc_type="pdf"` + `documents.flags.scan=true` 로 변경
- AC 3건 해당 수정
- §8 기획서 갱신 요청 표 §10.3 행 업데이트
- `/stats.slo_buckets` 의 버킷 name `pdf_scan` 은 메트릭 키 (DB 와 무관) 로 유지

---

## 4. 발견한 이슈 / 수정된 명세

### 4.1 DB CHECK 위반 사전 방지 (명세 v0.2.1 반영)

- **발견**: `documents.doc_type` CHECK 제약은 `('pdf','hwp','hwpx','docx','pptx','image','url','txt','md')` 9종. 명세 v0.2 §3.A′ 의 `doc_type="pdf_scan"` 은 위반
- **수정**: `doc_type="pdf"` 유지 + `flags.scan=true` 로 구분. 프론트 `DocType` 유니온도 무변경 (7번째 미사용 이슈 없음)

### 4.2 pyhwp 의 PyPI 이름 vs Python import 이름 불일치

- **발견**: PyPI `pyhwp` → Python `from hwp5 import ...`
- **참고**: W2 Day 4 (F 항목, HWP 구포맷) 착수 시 `HwpLegacyParser` 에서 `from hwp5 import binmodel, hwp5html` 형태로 import
- **CLI 경로**: `uv run hwp5html ...` 으로 subprocess 호출 가능 (0.1b15 확인)

### 4.3 Gemini `_attach_images` 의 mime 하드코딩 (잠재 이슈)

- **현재 상태**: `api/app/adapters/impl/gemini_llm.py:109` — 모든 이미지를 `image/png` 로 전달
- **영향 범위**: LLMProvider.complete(images=...) 경로. 현재 이 경로는 tag_summarize 스테이지에서만 사용되며 이미지 첨부 None — 기능적 문제 **현재 없음**
- **조치**: docstring 에 "레거시, 이미지 첨부 시 PNG 고정" 명시. Vision 정식 호출은 `GeminiVisionCaptioner` (W2 Day 2 구현) 에서 mime 정확 전달

### 4.4 Gemini 모델 이미 2.5 Flash

- **발견**: `_DEFAULT_MODEL = "gemini-2.5-flash"` (기존). DE-23 사실상 선반영
- **조치**: 명세 v0.2 §4 DE-23 을 "기존 코드에 이미 반영됨, docstring 만 교정" 으로 축소 — Day 1 기록에 남김 (명세 파일 자체는 영향 없음)

---

## 5. 남은 W2 Day 1 (이관)

다음 날 (2026-04-29 W2 공식 Day 1) 에 즉시 착수 가능한 상태로 만들어 뒀다. **W2 Day 2 첫 작업**:

1. **SLO 회복 (§3.A)** — `POST /documents` 흐름 재구성
   - 스트리밍 바이트 카운터 + SHA-256 on-the-fly
   - file magic MIME 검증 (python-magic 추가 승인 필요 여부 판단)
   - pending path 패턴 (`storage_path="pending/{uuid}.{ext}"`)
   - Tier 1 dedup 은 수신 응답 안에서 유지
   - BackgroundTask 가 실 Storage upload + pipeline 실행
2. **`/stats.slo_buckets` 집계** — 수신시각·완료시각 샘플 카운트 + p95_ms + pass_rate
3. **8-stage 전환 준비** — `STAGE_ORDER` 에 `content_gate` 자리 예약 (실제 스테이지 구현은 Day 5 G 항목)
4. **평가셋 시드 수집** — 공공 HWPX 10건 + 메신저·문서 스샷 20건 + 스캔 PDF 1건

### 5.1 Day 2 착수 전 추가 결정 필요

- **python-magic** 의존성 추가 여부 (file magic 검증) — 사용자 사전 승인 필요
- `pending path` 의 `storage_path` 값 형식 — `pending/{uuid}.{ext}` vs `pending/{user_id}/{uuid}.{ext}`

---

## 6. 커밋 단위 (계획)

Day 1 변경을 3 커밋으로 분할 (명세 v0.2 §7.2 커밋 분할 가이드 변형 — Day 1 축소 범위 반영):

1. `chore(deps): trafilatura · python-hwpx · pyhwp 사전 승인 후 추가 (DE-20 · W2 Day 1)`
2. `refactor(adapters): VisionCaptioner Protocol 분리 + Gemini 2.5 Flash docstring 교정 (DE-19 · DE-23)`
3. `docs(work-log, specs): W2 Day 1 진행 기록 + v0.2.1 보정 (pdf_scan → flags.scan) + /evals 디렉토리 시드`

---

## 7. `/doc/[id]` 경량판 (F′-α2) 결정 로그 (2026-04-24)

### 7.1 배경

- 사용자 요청 (2026-04-24): 업로드 완료 후 `/ingest` 에 멈춰있지 말고 "문서 업데이트가 어떻게 됐는지 보여주는 페이지" 로 이동 + "그곳에서 바로 검색"
- 초기 제안 편차로 내가 D → E → F′ 로 연달아 입장을 바꿈 (프로세스 오류). 사용자가 "정신 차리고 에이전트 활용해서 확실하게 정리" 를 지시
- 대안 6개 (A 풀 S3 / B 홈 단순 / C 전용 페이지 / D 검색 라우팅 / E 홈 강조 / F′ 경량 `/doc/[id]`) 를 동일한 평가 기준으로 3-agent 교차검증

### 7.2 3-agent 교차검증 결과

| 관점 | 1위 | 점수 | 핵심 근거 |
|---|---|---|---|
| senior-planner | **F′** | 32/35 | 사용자 원문 singular 의도 정확 대응, W2→W4 이중투자 0, Golden Path 3종 모두 자연스러움 |
| senior-qa | **F′** | 29/35 (조건부) | 변경 최소, URL 공유·새로고침·뒤로가기 내구성. **단 "백엔드 해석 재조정" 필요** |
| senior-developer | **F′** | 26/35 → D4 가중치 적용 시 1위 | W4 `/doc/[id]` URL 계약 선점. W2+W4 총 공수 최소 |

공통 부결: **A (풀 S3, W2 범위 초과)**, **D 단독 (검색 API 계약 파괴, QA CRITICAL)**.
부분 충족: B·C·E (사용자 원문 "보여주는 페이지" 의도 불충분 + W4 이중 투자).

### 7.3 F′ 세부 변종 선택 (α1 / α2 / β)

3 에이전트 간 미세 불일치 발견 — 백엔드 구현 방식:

| 변종 | 내용 | 공수 | 판정 |
|---|---|---|---|
| F′-α1 | 기존 `GET /documents/{id}/status` + list 재활용, 백엔드 변경 0 | 0.4d | QA 선호. 단 title/tags/summary 별도 fetch 필요 → list 전체 받기 비효율 |
| **F′-α2** | **신규 `GET /documents/{id}` 단건 조회 엔드포인트** | **0.6d** | **기획자·개발자 공통 지지. QA 도 수용** (부결 대상 아님) |
| F′-β | D 의 `/search?highlight_doc_id=` 분기 재활용 | 1.1d | **QA CRITICAL 부결** — 검색 API 계약 파괴, W3 하이브리드 재설계 시 충돌 |

**채택: F′-α2**. α1 대비 깨끗한 API 계약, β 대비 검색 API 오염 0, W4 `/doc/[id]` 본격판에 그대로 재사용.

### 7.4 확정된 세부 결정 (사용자 승인 2026-04-24)

| # | 항목 | 결정 | DE 번호 |
|---|---|---|---|
| 1 | 자동 이동 정책 | 단일 파일 = 자동 이동, 다중 = 첫 완료만 자동 + 나머지 "상세 보기" 링크 | DE-28 |
| 2 | `/doc/[id]` Hero 검색 scope | 전역 검색 (W4 에 "이 문서 안에서" 토글 이월) | DE-27 |
| 3 | `duplicated=true` 처리 | `/doc/<existing_id>` 이동 + "이미 등록된 문서" toast | §3.M 본문 명시 |

### 7.5 명세 v0.3 반영 범위

- §2 범위 재검토 표 — 항목 M 추가 (P1, 0.6d, Day 6)
- §2.1 버퍼 전략 — Day 6 = M 전용. 리스크 발화 시 M W3 이관
- §3.M 신설 — Why / What / 엣지 케이스 / AC 12건
- §4 DE-27 · DE-28 신설 RESOLVED
- §7.4 DoD — 항목 M AC 12건 추가
- §8 기획서 갱신 요청 — §7 S3 경량판 선편입 주석 추가
- §17 버전 이력 — v0.3 편입

### 7.6 W2 Day 6 착수 예정 작업 (F′-α2 구현)

**PR #1 — 백엔드** (0.2d)
- `api/app/routers/documents.py` 에 `GET /documents/{id}` 추가
- `_build_document_list_item` 추출 (기존 `list_documents` 내부 로직 재활용)
- 404 / failed 문서 조회 허용

**PR #2 — 프론트 경량 페이지** (0.3d)
- `web/src/app/doc/[id]/page.tsx` (Server Component, `params: Promise<{id: string}>` Next.js 16 컨벤션)
- `web/src/components/jet-rag/doc-summary-header.tsx` 신규
- Hero 검색 (기존 컴포넌트 재사용) → `/search?q=` submit
- `/doc/<존재X>` → `notFound()`
- `web/src/lib/api/` 에 `getDocument(id)` 함수 추가

**PR #3 — 인제스트 자동 이동** (0.1d)
- `web/src/app/ingest/page.tsx` 완료 감지 effect
- `routedRef` 로 중복 라우팅 방지
- `duplicated=true` → `/doc/<existing_id>` + toast
- `status="failed"` → 이동 X

**PR #4 — 회귀 테스트 + work-log** (0.0d, PR #1~3 내 분산)

### 7.7 번복 없음 약속

F′-α2 는 3-agent 교차검증에서 **기획·QA·개발 모두 1위 + α2 변종 최적** 결론. 사용자 재검토 요구 시 **같은 에이전트 3개를 재실행** 해 결과를 비교한다. 내 즉흥 판단으로 철회하지 않는다.

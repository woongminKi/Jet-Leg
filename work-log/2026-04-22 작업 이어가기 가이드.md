# 2026-04-22 작업 이어가기 가이드 (Handoff)

> 회사 컴퓨터에서 기획 단계까지 완료. 집 개인 컴퓨터에서 W1 착수 이어가기 위한 문서.
>
> **이 문서 한 장만 보면 다음 세션에서 어디서부터 시작하면 되는지 알 수 있도록 작성.**

---

## 1. 현재까지 완료된 것 (회사 컴퓨터, 2026-04-22)

- ✅ 기획서 v0.1 rev2 완성 (18개 섹션)
- ✅ 즉시 결정 항목 DE-01~04 + DE-03a 확정
- ✅ 프로젝트 폴더 생성 및 rename (`personal-knowledge-agent` → `Jet-Leg`)
- ✅ 기술 스택·어댑터 구조·인제스트 파이프라인·KPI·6주 로드맵 확정
- ✅ MVP 6주 일정: 2026-04-22 착수 ~ 2026-06-02 완료

## 2. 지금 블로커 (딱 하나)

**커밋 이메일 결정** — 회사 이메일(`woongmin@forsit.co.kr`)이 글로벌로 설정되어 있어 포트폴리오 공개 repo에 쓰면 회사 이메일이 영구 노출됨.

**선택지** (집에서 결정):
- **A. 개인 이메일** — `git config --local user.email "personal@..."` (repo-local로만 설정)
- **B. GitHub noreply** — `<id>+woongminKi@users.noreply.github.com` (추천, 실주소 노출 없음)
- **C. 회사 이메일 유지**

집 컴퓨터에서 글로벌 설정이 다를 수 있으니 **로컬 repo 단위로 명시 설정** 권장.

---

## 3. 집 컴퓨터 사전 설치 체크리스트

W1 착수 전에 설치 완료되어 있어야 할 것:

### 3.1 필수 도구

| 도구 | 설치 명령 | 버전 확인 |
|---|---|---|
| Homebrew (macOS) | — | `brew --version` |
| Git | `brew install git` | `git --version` |
| GitHub CLI | `brew install gh` | `gh --version` |
| Python 3.12 | `uv python install 3.12` (uv 설치 후) | `python --version` |
| **uv** (Python 관리자) | `curl -LsSf https://astral.sh/uv/install.sh \| sh` | `uv --version` |
| Node.js 20+ | `brew install node` | `node --version` |
| **pnpm** | `npm install -g pnpm` | `pnpm --version` |
| VS Code 또는 Cursor | `brew install --cask cursor` | — |

### 3.2 GitHub CLI 인증 (개인 계정으로)

```bash
gh auth login
# → GitHub.com → HTTPS → 브라우저로 로그인 (개인 계정!)
gh auth status   # 개인 계정 확인
```

**주의**: 회사 컴퓨터의 `woongminKi` 계정이 아니라, **개인 포트폴리오용 GitHub 계정**으로 로그인. 혹시 `woongminKi`가 개인 계정이면 그대로 OK.

---

## 4. 필요한 외부 서비스 키 (무료 티어)

개인 계정으로 모두 가입·API 키 발급:

| 서비스 | 용도 | 가입 URL | 환경변수명 |
|---|---|---|---|
| **Supabase** | DB + Storage + pgvector | https://supabase.com | `SUPABASE_URL`, `SUPABASE_KEY` |
| **Google AI Studio** | Gemini 2.0 Flash | https://aistudio.google.com | `GEMINI_API_KEY` |
| **Hugging Face** | BGE-M3 Inference | https://huggingface.co | `HF_TOKEN` |
| Railway | Backend 배포 (나중) | https://railway.app | — |
| Vercel | Frontend 배포 (나중) | https://vercel.com | — |

W1 Day 1~2에서 **Supabase + Gemini 두 개만** 있으면 시작 가능. 나머지는 해당 주차 들어갈 때 준비.

---

## 5. 파일 동기화 (회사 → 집)

현재 `/Users/kiwoongmin/Desktop/documents/test-repo/image2/piLab/Jet-Leg/` 폴더에 기획 문서 2개가 있음:
- `work-log/2026-04-22 개인 지식 에이전트 기획서 v0.1.md`
- `work-log/2026-04-22 작업 이어가기 가이드.md` (이 파일)

### 옵션 A — iCloud Drive / Dropbox / 구글 드라이브
`Jet-Leg/` 폴더를 통째로 클라우드에 업로드 → 집에서 다운로드.
**주의**: 회사 컴퓨터에서 개인 클라우드 동기화가 차단되어 있지 않은지 확인.

### 옵션 B — USB 드라이브
폴더 전체 복사.

### 옵션 C — 이메일 첨부
기획서 md 2개만 이메일로 자신에게 전송. 폴더 구조는 집에서 수동 재생성. **가장 안전**.

### 옵션 D (추천) — GitHub **비공개** repo 임시 경유
집에서 어차피 GitHub 쓸 것. 회사에서 임시 private repo 만들어 md 2개만 commit/push → 집에서 clone → 이후 public 전환.
**장점**: 나중에 작업 이어갈 때 히스토리 유지. **주의**: 회사 계정으로 push하지 말 것 — private이라도 커밋 이메일이 회사 이메일로 박힘.

→ **D도 커밋 이메일 문제가 있어서 까다로움**. **옵션 C (이메일 첨부)가 가장 깔끔**.

---

## 6. 집에서 W1 Day 1 실행 순서

사전 설치(§3)와 Supabase·Gemini 키(§4) 준비된 가정.

### Step 1 — 작업 디렉터리 세팅
```bash
mkdir -p ~/projects
cd ~/projects
# 회사 컴퓨터에서 받은 md 2개를 ~/projects/Jet-Leg/work-log/ 에 배치
mkdir -p Jet-Leg/work-log
# (이메일로 받은 md 2개를 이 폴더로 이동)
cd Jet-Leg
```

### Step 2 — Claude Code 실행 및 컨텍스트 부여
```bash
claude
```

첫 프롬프트 (복붙용):
```
이전 작업을 이어가려고 해.
work-log/2026-04-22 개인 지식 에이전트 기획서 v0.1.md 와
work-log/2026-04-22 작업 이어가기 가이드.md 를 읽고
W1 Day 1부터 착수해줘. 내 GitHub 계정은 [개인계정명], 커밋 이메일은 [선택한 이메일].
```

### Step 3 — W1 Day 1 작업 (Claude가 순차 진행)
1. `.gitignore`, `LICENSE (MIT)`, `README.md` 초안 작성
2. `api/`, `web/`, `evals/`, `docs/` 빈 디렉터리 + `.gitkeep`
3. `git init` + `git config --local user.email <결정한 이메일>`
4. GitHub repo 생성 (`gh repo create Jet-Leg --public --description "..."`)
5. 초기 커밋 + push

### Step 4 — Day 1 DoD 확인
- [ ] GitHub 공개 repo 존재, README·LICENSE·.gitignore 커밋됨
- [ ] 커밋 이메일이 의도한 값으로 찍혀 있음 (`git log --format="%ae"` 확인)
- [ ] 모노레포 구조 (`api/`, `web/`, `evals/`, `docs/`, `work-log/`) 존재
- [ ] 본 문서 업데이트 (Day 1 완료 표기)

---

## 7. 참조 파일 경로 (집 컴퓨터 기준)

```
~/projects/Jet-Leg/
├── work-log/
│   ├── 2026-04-22 개인 지식 에이전트 기획서 v0.1.md  ← 마스터 기획서
│   └── 2026-04-22 작업 이어가기 가이드.md           ← 이 파일
├── api/          (Day 2부터 생성)
├── web/          (Day 6부터 생성)
├── evals/        (W5부터)
├── docs/         (Day 1부터 ADR 시작)
├── .gitignore    (Day 1)
├── LICENSE       (Day 1, MIT)
└── README.md     (Day 1 초안)
```

---

## 8. 주의사항 (집에서 작업 시)

1. **`.env` 파일 절대 커밋 금지** — `.gitignore` 최상단에 명시
2. **API 키 하드코딩 금지** — 코드에 직접 `GEMINI_API_KEY = "..."` 넣지 말 것
3. **커밋 전 `git config --get user.email` 로 이메일 재확인**
4. **공개 repo에 민감 정보 금지** (§15.1-b 규칙)
5. **평가 데이터셋은 공개 가능한 자료만**

---

## 9. 남은 주요 결정 (요약)

| 시점 | 항목 |
|---|---|
| 집 도착 즉시 | 커밋 이메일 선택 (A/B/C) |
| W1 Day 1 | (결정된 것 없음, 스캐폴드 진행) |
| W1~2 중 | DE-05(iOS Shortcuts 스펙), DE-06(Vision 다운스케일), DE-07(변경점 diff 범위) |
| W3~4 중 | DE-08~11 |
| W5 | DE-12~13 |

전체 리스트는 기획서 §15 참조.

---

## 10. 주요 결정 치트시트 (회사에서 확정된 것)

| 항목 | 값 |
|---|---|
| 제품명(임시) | ""Jet-Leg"" |
| 코드 슬러그 | `Jet-Leg` |
| MVP 시작 | 2026-04-22 |
| MVP 완료 목표 | 2026-06-02 |
| GitHub 공개 | ✅ (개인 계정) |
| 배포 유지 | ✅ (MVP 후에도) |
| 라이선스 | MIT |
| Backend | FastAPI (Python 3.12, uv) |
| Frontend | Next.js + PWA (pnpm) |
| DB | Supabase (Postgres + pgvector + Storage) |
| LLM (MVP) | Gemini 2.0 Flash (무료) |
| Vision (MVP) | Gemini 2.0 Flash 내장 |
| 임베딩 (MVP) | BGE-M3 via HF Inference API |
| BM25 (MVP) | BGE-M3 sparse 출력 |
| Evaluation | Ragas |
| 어댑터 스텁 | OpenAI (LLM/Embedding/Vision 각 1개) |

---

## 11. 다음 세션 프롬프트 (복붙용)

### 옵션 1 — 한 번에 컨텍스트 전부 주입
```
이 프로젝트는 포트폴리오용 RAG 기반 개인 지식 에이전트 ""Jet-Leg""(Jet-Leg)야.
회사 컴퓨터에서 기획을 완료하고 집으로 옮겨왔어. 지금 W1 Day 1부터 착수할 거야.

먼저 다음 두 문서를 읽어줘:
- work-log/2026-04-22 개인 지식 에이전트 기획서 v0.1.md
- work-log/2026-04-22 작업 이어가기 가이드.md

그다음 이어가기 가이드 §6 "집에서 W1 Day 1 실행 순서" 에 따라 진행해줘.
내 GitHub 계정은 [___], 커밋 이메일은 [B: noreply / A: 개인 / C: 회사] 중 [___] 로 갈게.
Supabase·Gemini 키는 아직 없어 — 해당 단계에서 안내해줘.
```

### 옵션 2 — Claude에게 가이드부터 읽히기
```
work-log/2026-04-22 작업 이어가기 가이드.md 읽고, 지금부터 W1 Day 1 착수 도와줘.
```

---

## 12. 문제 해결 FAQ

### Q. 집 컴퓨터에 uv·pnpm·gh 없음
→ §3.1 표대로 순차 설치. Homebrew 먼저.

### Q. md 파일을 이메일로 보낸다면 한글 파일명이 깨짐
→ zip으로 압축해서 전송, 또는 텍스트 에디터로 열어 내용 복사 후 집에서 새 파일 생성.

### Q. Supabase에서 pgvector 어떻게 켜나
→ Supabase 프로젝트 대시보드 → Database → Extensions → `vector` 검색 → Enable.

### Q. Gemini 무료 한도 초과 시
→ 기획서 §D-7-3 참조. 이미지 해상도 1024px 다운스케일 정책으로 대응.

### Q. 회사 컴퓨터 기록을 지우고 싶다
→ `/Users/kiwoongmin/Desktop/documents/test-repo/image2/piLab/Jet-Leg/` 폴더 삭제. piLab git repo의 untracked 상태이므로 git에는 남지 않음.

---

## 13. 버전 이력

| 버전 | 일자 | 변경 |
|---|---|---|
| v1 | 2026-04-22 | 최초 작성 (회사 컴퓨터에서 집으로 handoff) |

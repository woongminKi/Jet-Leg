# 2026-04-24 오늘 작업 종합 정리

> 하루 동안 (1) W1 마감 검증 체크리스트 → (2) **W2 명세 v0.1 → v0.2 → v0.3** (3단계 반복 개선) → (3) **W2 Day 1 선행 착수** (의존성·어댑터 골격·/evals) → (4) **`/doc/[id]` 경량판 (F′-α2) 결정** 까지 진행. **origin 35 커밋 반영** (어제 종료 시점 26 → 오늘 +9). 내일은 **§0 부터 읽고** Day 2 착수하면 됨.

---

## 0. 내일 세션 시작 시 이것부터 (체크리스트)

> 작성 시점 (2026-04-24 종료) 기준 스냅샷. 의심되면 §0.1 로 검증.

### 0.1 현재 상태 확인 (1분)
```bash
cd ~/Desktop/piLab/Jet-Rag
git status                          # working tree clean 인지
git log --oneline origin/main..HEAD # 로컬 미반영 커밋 (오늘 종료 시점 0 예상)
```

### 0.2 다른 머신이라면
- **`work-log/2026-04-23 작업 이어가기 가이드.md`** 의 §3 ~ §7 따라하기 (도구·env·supabase)
- 프론트 `cd web && cp .env.example .env.local && pnpm install`
- **오늘 추가된 의존성**: `cd api && uv sync` 한 번 실행 (trafilatura · python-hwpx · pyhwp)

### 0.3 알려진 데이터 이슈 (작성 시점)
- **sonata-the-edge_catalog 데이터**: 사용자가 Supabase Studio SQL Editor 에서 직접 삭제 완료 (2026-04-24 오후, "잘 됐어" 회신)
- Storage bucket `documents` 의 해당 blob 파일 삭제 확인 필요 (SQL 은 row 만 지움) — 내일 진입 시 Supabase Studio → Storage → documents 에서 `sonata` 이름 파일 잔여 확인 후 수동 삭제
- 나머지 failed doc 0건

### 0.4 Day 2 착수 전 결정 필요 2건
명세 v0.3 §7.1 Day 2 착수 순서대로. 둘 다 사용자 결정:

1. **python-magic 의존성 추가 승인 여부** — SLO 회복 시 file magic MIME 검증용. `.png` rename 한 exe 차단 AC (§3.A AC) 를 위해 필요. 미승인 시 확장자 whitelist + content-type header 검증으로 축소 구현
2. **pending path 형식** — `pending/{uuid}.{ext}` (단순) vs `pending/{user_id}/{uuid}.{ext}` (W5 멀티유저 대비) 중 택

### 0.5 Day 2 진입 순서 (명세 v0.3 §7.1 그대로)

| # | 액션 | 예상 시간 |
|---|---|---|
| 1 | Day 2 결정 2건 확정 (python-magic, pending path) | 10분 |
| 2 | 항목 A (SLO 회복) 본격 구현 — pending path + SHA-256 스트리밍 + 50MB 카운터 + `/stats.slo_buckets` | **3~4시간** |
| 3 | 항목 B (Vision) 실제 호출 구현 — Gemini 2.5 Flash JSON 프롬프트 + retry + 다운스케일 | 2~3시간 |
| 4 | 평가셋 실제 샘플 수집 — `/evals/hwpx_samples/` 공공 HWPX 10건 + `/evals/vision_samples/expected.jsonl` + 스샷 20건 | 1시간 |
| 5 | Day 2 work-log 작성 | 30분 |

### 0.6 W2 Day 6 (F′-α2) 착수 조건
- Day 5 까지 리스크 발화 (Vision 한도 / HWPX 실패율 30%+ / 게이트 오탐 / 스캔 감지 오탐) 없으면 → Day 6 에 **`/doc/[id]` 경량판** 착수 (명세 v0.3 §3.M, 0.6d)
- 리스크 발화 시 → **M 은 W3 Day 1 이관**, Day 6 은 회수에 사용

---

## 1. 오늘 커밋 한눈에 (9 커밋)

| SHA | 영역 | 내용 |
|---|---|---|
| `423c494` | docs | 2026-04-24 W1 마감 검증 체크리스트 + W2 진입 준비 |
| `f673162` | chore | `.gitignore` 에 로컬 참고용 wireframe 원본 제외 |
| `e0804cf` | docs | W2 스프린트 명세 v0.1 (senior-planner 작성) |
| `1f9c300` | docs | W2 명세 QA 검수 리포트 + DE-17·20 선행 조사 |
| `9f1c608` | docs | W2 스프린트 명세 v0.2 — QA 검수 결과 반영 |
| `424d0b7` | chore | `trafilatura` · `python-hwpx` · `pyhwp` 사전 승인 후 추가 |
| `c352f2d` | refactor | `VisionCaptioner` Protocol 분리 + Gemini 2.5 Flash docstring 교정 |
| `4fa92ed` | docs | W2 Day 1 진행 기록 + 명세 v0.2 보정 (pdf_scan → flags.scan) + /evals 시드 |
| `9b1b955` | docs | W2 명세 v0.3 — `/doc/[id]` 경량판 (F′-α2) 편입 |

---

## 2. 오늘 달성 전체 결과

### 2.1 W1 마감 검증 체크리스트 (브라우저 10건) 발행
- `work-log/2026-04-24 W1 마감 검증 + W2 진입 준비.md` §2
- 사용자 직접 실행 예정 (별도 작업)
- 시나리오 1~10 + sonata 회복 경로 명시

### 2.2 W2 명세 진화 (v0.1 → v0.3)

| 버전 | 작성자 | 변경 |
|---|---|---|
| **v0.1** | senior-planner | 초안, 범위 표, DE-17~22, 리스크·완화 |
| **v0.2** | senior-qa 검수 반영 | **CRITICAL 1 + MAJOR 14 + MINOR 4** 해소. 파이프라인 8-stage 확정, SLO + pending path, KPI 산식, Gemini 2.5 교체 (DE-23), 스캔 PDF 재라우팅 편입, I(DOCX) W3 이월 |
| v0.2.1 | Day 1 선행 착수 중 | DE-17·20 RESOLVED, §3.A′ `doc_type="pdf_scan"` → `flags.scan=true` 보정 |
| **v0.3** | 3-agent 교차검증 | 항목 M `/doc/[id]` 경량판 (F′-α2) Day 6 편입, DE-27·28 신설 |

### 2.3 W2 Day 1 선행 착수 (오늘 2026-04-24)

**완료된 것** (상세 `work-log/2026-04-24 W2 Day 1.md`):
- 의존성 3종 설치 — trafilatura 2.0.0 · python-hwpx 2.9.0 · pyhwp 0.1b15 Python 3.12 호환성 통과 → **DE-20 RESOLVED**
- `VisionCaptioner` Protocol 분리 (`adapters/vision.py` + `adapters/impl/gemini_vision.py` 신설) — 어댑터 6종 재편 (DE-19 반영) 골격
- Gemini 2.0 Flash docstring → 2.5 Flash 로 교정 (**DE-23** 실제 모델은 이미 2.5 였음, 주석만 교정)
- `/evals/{hwpx,vision,pdf}_samples/` 디렉토리 + `vision_samples/expected.schema.json` (JSON Schema Draft 2020-12)
- 명세 v0.2.1 보정 — `doc_type="pdf_scan"` 은 DB CHECK 위반, `doc_type="pdf"` + `flags.scan=true` 로 변경

**Day 2 이관된 것** (오늘 손 안 댐):
- SLO 회복 (항목 A) — 큰 구조 변경 (pending path + SHA-256 스트리밍 + file magic)
- `/stats.slo_buckets` 집계
- 8-stage `content_gate` 실제 스테이지 구현 (Day 5 G 항목과 묶음)
- 평가셋 실제 샘플 수집

### 2.4 `/doc/[id]` 경량판 결정 (F′-α2)

**배경**: 사용자가 업로드 완료 후 결과 페이지로 이동 + 바로 검색 가능 요구.

**프로세스 오류 + 복구**:
- 내가 D → E → F′ 로 **3번 입장을 바꿈** (프로세스 실패)
- 사용자 지시로 **3-agent 교차검증** 도입 — senior-planner, senior-qa, senior-developer 독립 판정
- 전원 **F′ 1위** 일치, **변종 α2** (신규 `GET /documents/{id}` 엔드포인트) 채택

**확정 스펙** (명세 v0.3 §3.M, 0.6d, Day 6):
- 백엔드: `GET /documents/{id}` 단건 조회 엔드포인트 신설
- 프론트: `/doc/[id]` 경량 라우트 + `DocSummaryHeader` + Hero 검색 (전역)
- 인제스트: 단일=자동 이동, 다중=첫 완료만 자동 + 나머지 "상세 보기" 링크
- `duplicated=true` → `/doc/<existing_id>` 이동 + toast

---

## 3. 핵심 결정 + 근거 (오늘 확정분)

| 결정 | 채택 값 | 근거 |
|---|---|---|
| **DE-17** HEIC 변환 | `pillow-heif` 불요. Gemini 2.5 Flash 가 HEIC/HEIF 직접 지원 | Vertex AI 공식 문서 |
| **DE-18** trafilatura + SSRF | trafilatura 채택. SSRF 블랙리스트 `127/10/172.16/192.168/169.254` | 무료 티어 원칙, 표준 라이브러리 |
| **DE-19** 어댑터 재편 | `VisionCaptioner` 분리, 6종 대분류 | Gemini Vision 과 텍스트 LLM 은 교체 단위 달라야 |
| **DE-20** pyhwp | Nix 시스템 패키지 X, Python 의존성. Railway 자동 처리 | pyhwp 는 PyPI 패키지 (import: `hwp5`, CLI: `hwp5html`) |
| **DE-21** 입력 게이트 오탐 | 주민번호 (b) 경량 (생년월일 검증), 카드번호 (a) 단순 | MVP 균형 |
| **DE-22** SHA-256 계산 | 수신 스트리밍 on-the-fly | Tier 1 dedup UX 보존 |
| **DE-23** Gemini 모델 | 2.5 Flash | 2.0 Flash 2026-06-01 deprecation. 코드는 이미 반영됨 (docstring 만 오인용) |
| **DE-24** storage_path 정책 | pending path `pending/{uuid}.{ext}` — migration 불요 | DB CHECK `NOT NULL` 유지 |
| **DE-25** 경고 UX localStorage | `jetrag.warnings.{pii|watermark|third_party}.muted` 카테고리 전역 | MVP 단순 |
| **DE-26** 월 업로드 상한 | W4 S1 홈 MyDocStats 배너 | 포지셔닝 유지 (§11.5) |
| **DE-27** `/doc/[id]` scope | 전역 검색 (W4 "이 문서 안에서" 토글 이월) | 백엔드 변경 0, UX 일관 |
| **DE-28** 자동 이동 정책 | 단일=자동, 다중=첫 완료만 자동 + 나머지 링크 | Primary GP 단일 파일 시나리오 기본, Tertiary 도 보통 단일 |
| **프로세스** 3-agent 교차검증 | 중요 결정 시 기본 패턴 | 내가 즉흥 판단 반복 실패 (D→E→F′) 에서 배움 |

---

## 4. 발견·해결한 Critical 이슈 (오늘)

| # | 이슈 | 발견 경로 | 처리 |
|---|---|---|---|
| 1 | 파이프라인 스테이지 모순 — 기획서 §10.2 플로우 vs W1 실구현 7-stage vs 명세 "단계 6 content_gate" 가 전부 다른 순서 | senior-qa 검수 CRITICAL A-1 | 명세 v0.2 에 **8-stage 확정** (`extract → chunk → content_gate → tag_summarize → load → embed → doc_embed → dedup`), Vision 은 ImageParser 내부 composition |
| 2 | `doc_type="pdf_scan"` 은 DB CHECK 위반 | Day 1 실제 코드 읽으며 발견 | 명세 v0.2.1 에 `doc_type="pdf"` 유지 + `flags.scan=true` 로 변경 |
| 3 | Gemini 모델 docstring 오인용 (실제는 이미 2.5 Flash) | Day 1 실측 | docstring 만 교정, 실제 호출 영향 0 |
| 4 | pyhwp PyPI 이름 vs Python import 이름 불일치 | `uv add pyhwp` 후 smoke | 기록: import 는 `from hwp5 import ...`, CLI 는 `hwp5html` |
| 5 | `storage_path NOT NULL` + BackgroundTask 구조 충돌 | senior-qa B-2 | pending path 패턴 (DE-24) 으로 해소 |
| 6 | `POST /documents` 의 `url` 필드 추가 시 422 | senior-qa B-4 | `POST /documents/url` 별도 엔드포인트 신설 (명세 v0.2 §3.E) |
| 7 | 프로세스 실패 — D→E→F′ 연쇄 입장 변경 | 사용자 지적 "말 계속 바뀌어" | 3-agent 교차검증 도입, F′-α2 최종 확정. 이후 동일 패턴으로 중요 결정 처리 |

---

## 5. 알려진 한계 / 내일+ 진입점

### 내일 (Day 2) 배치
- Day 2 결정 2건 (python-magic, pending path 형식)
- 항목 A SLO 회복
- 항목 B Vision 실제 호출
- 평가셋 샘플 수집

### W2 후반 (Day 3~7)
- Day 2~3: HWPX 파서
- Day 3: 이미지 파서 + 스캔 PDF 재라우팅
- Day 4: URL 파서 + HWP 구포맷
- Day 5: 입력 게이트 B + batch 폴러 (병렬)
- Day 6: `/doc/[id]` 경량판 (F′-α2) — 리스크 발화 시 W3 이관
- Day 7: DoD 검수 + W2 종합 정리

### W3~W4 이월 (명세 v0.3 §6)
- I DOCX · J PPTX · K 쿼리 분석 (W3)
- 하이브리드 검색 RRF · 변경점 diff Tier 3 (W3)
- `/doc/[id]` 본격판 (청크 리스트·출처 뷰·태그 편집) (W4)
- DE-21 Luhn 체크 고도화 · DE-25 localStorage 토글 · DE-26 월 상한 배너 (W4)

---

## 6. 일자별 상세 문서 (오늘 신설 5건)

| 문서 | 내용 |
|---|---|
| `2026-04-24 W1 마감 검증 + W2 진입 준비.md` | W1 마감 브라우저 검증 체크리스트 10건 + W2 범위 초안 |
| `2026-04-24 W2 스프린트 명세.md` | **v0.1 → v0.3 전체 명세** (최종 CONFIRMED) — 7일 일정 · P0 5 + 평가셋 + P1 5 + P2 1 · DE-17~28 · 항목 A~M 상세 |
| `2026-04-24 W2 명세 QA 검수 및 선행 조사.md` | senior-qa 검수 결과 요약 + DE-17/20 선행 조사 결과 |
| `2026-04-24 W2 Day 1.md` | 오늘 실제 진행 기록 — 환경 조사 · 의존성 · 어댑터 골격 · F′-α2 결정 로그 |
| **`2026-04-24 오늘 작업 종합 정리.md`** (이 문서) | 하루 전체 요약 + 내일 진입점 §0 |

### 기존 (어제까지) 참고 문서
- `2026-04-22 개인 지식 에이전트 기획서 v0.1.md` (마스터)
- `2026-04-23 W1 Day 6-7+ 종합 정리.md` (어제 종합정리 — §0 패턴 차용 원본)
- `2026-04-23 W1 Day 7+ 실패 처리 정책.md` 등

---

## 7. 회고 한 줄

오늘은 **"명세 품질 + 프로세스 학습"** 의 날. senior-planner 초안 → senior-qa 검수 (CRITICAL 1 + MAJOR 14 발견) → v0.2 반영 패턴을 확립했고, 이어 **"즉흥 판단 반복 실패 → 3-agent 교차검증" 프로세스** 를 경험적으로 도입. 내일 W2 Day 2 부터는 명세 v0.3 §7.1 순서대로 진행하면 됨.
